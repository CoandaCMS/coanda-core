filter:
    paths: [src/*]
    excluded_paths: [vendor/*, tests/*]
before_commands:
    - 'composer install --dev --prefer-source'
tools:
    php_mess_detector: true
    php_analyzer: true
    php_loc: true
    php_sim: true